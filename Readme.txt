"make bonus" would make bonus files separately from the base ones. 
In later projects, such as pipex, both bonus of libft and get_next_line are added to "base" libft so that gnl may drop get_next_line_utils and just use already-existing support functions.
